<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>TJ no Gini(<%= @songs.count %>)</title>
  </head>
  <body>
    <center style="font-size:20px;color:#000;">
      <% @songs.each do |song| %>
      <p>
        <span>id : <%= song.id %></span><br>
        <span>tjnum : <%= song.song_tjnum %></span><br>
        <span>title : <%= song.title %></span><br>
        <span>artist_name : <%= song.artist_name %></span><br><br>
        <!-- lyrics : <%#= song.lyrics.html_safe %> -->
      </p>
      <% end %>
    </center>
  </body>
</html>
